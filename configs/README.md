# Configurations

카메라·좌표 변환·로봇 실행에 필요한 설정 파일을 정리하는 디렉터리입니다.

예정 예시:

```text
configs/
├── camera_intrinsics.yaml
├── homography_workspace.yaml
└── experiment_config.yaml
```

실제 설정 파일 공개 전에는 개인 장치 경로, IP 주소, serial 정보가 포함되어 있는지 확인합니다.
